{
  "name": "nexus",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["gateway", "proto"],
  "scripts": {
    "gateway": "bun run --filter gateway dev"
  },
  "dependencies": {
    "@noble/hashes": "^2.0.1"
  }
}
