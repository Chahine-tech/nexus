import type { RankedResult } from "./MergeEngine"

// TODO: optional RAG pipeline (week 4+)
export async function ragPipeline(
  _query: string,
  _results: RankedResult[],
): Promise<string> {
  return ""
}
